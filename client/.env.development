# ── Local Development ─────────────────────────
# The Vite proxy (vite.config.js) forwards /api/* to localhost:8000
# So you DON'T need to set VITE_API_URL for local dev
VITE_APP_NAME=Velour
VITE_ENVIRONMENT=development
